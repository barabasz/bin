#!/bin/zsh

# load variables and functions
source $ZDOTDIR/.zsh_load

echo "☁️ 👉 🖥️ ${redb}Pulling all repositories from the remote.${reset}"

for repo in $(git-repos)
do
    printhead $(basename $repo)
    cd $repo
    echo "${cyan}$(git remote get-url origin)${reset}"
    git pull --rebase
done

