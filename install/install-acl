#!/bin/bash

if [[ "$(osname)" != "macos" ]]; then
    if [[ $(isinstalled getfacl) -eq 0 ]]; then
        # brew install -q acl
        aptinstall acl
    else
        getfacl --version
    fi
else
    echo "Linux is required for this software."
fi
